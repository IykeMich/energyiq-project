/**
 * Generated by orval v8.8.0 🍺
 * Do not edit manually.
 * EnergyIQ API
 * B2B Energy Supply Chain Platform API
 * OpenAPI spec version: 1.0
 */
import {
  useQuery
} from '@tanstack/react-query';
import type {
  QueryFunction,
  QueryKey,
  UseQueryOptions,
  UseQueryResult
} from '@tanstack/react-query';

import type {
  DocumentTypeListResponse,
  ErrorResponse
} from '../schemas';

import { fetcher } from '../../fetcher';


type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];



/**
 * Returns the authenticated supplier's active KYC document-type configuration.

**Frontend usage:** fetch this endpoint to render supplier/distributor KYC checklists, upload rules, required badges, expiry fields, and accepted-file hints; do not hard-code those rules in the frontend.

**Inputs:** no path, query, or body fields.

**Returned fields:** document_type is the stable machine key that must be sent when uploading a document; document_name is its display label; document_category identifies the checklist group; required controls whether submission is mandatory; expiry_required controls whether validity_period_months applies; max_file_size_mb and allowed_file_types define upload validation; audience controls which onboarding portal displays the item.

**Enum values:** audience is supplier, distributor, or both; allowed_file_types contains application/pdf, image/jpeg, or image/png.

**Checks:** results are tenant-isolated, include active records only, require a Bearer token, and never return another supplier's configuration. Supplier/staff callers require document-type list permission. A verified pending or pending-review distributor may read this exact endpoint solely to complete or review onboarding; no other document-type management permission is granted.

**Expected outcomes:** use data as the source of truth for KYC forms; an empty array means the supplier currently has no active KYC document types.

**Required inputs:**
None.

**Optional inputs:**
None.

**Authorization:**
Bearer JWT required. Account status, tenant isolation, and RBAC permissions are checked before the operation runs.

**Edge cases:**
- `401` — Missing, expired, or invalid authentication is rejected.
- `403` — The caller lacks the required permission, tenant access, or account state.
- `429` — A rate limit or resend cooldown applies; wait before retrying.
- `500` — Unexpected failures use the standard error envelope; retain user input for a safe retry.
 * @summary List KYC document types
 */
export type getV1DocumentTypeListResponse200 = {
  data: DocumentTypeListResponse
  status: 200
}

export type getV1DocumentTypeListResponse401 = {
  data: ErrorResponse
  status: 401
}

export type getV1DocumentTypeListResponse403 = {
  data: ErrorResponse
  status: 403
}

export type getV1DocumentTypeListResponse429 = {
  data: ErrorResponse
  status: 429
}

export type getV1DocumentTypeListResponse500 = {
  data: ErrorResponse
  status: 500
}

export type getV1DocumentTypeListResponseSuccess = (getV1DocumentTypeListResponse200) & {
  headers: Headers;
};
export type getV1DocumentTypeListResponseError = (getV1DocumentTypeListResponse401 | getV1DocumentTypeListResponse403 | getV1DocumentTypeListResponse429 | getV1DocumentTypeListResponse500) & {
  headers: Headers;
};

export const getGetV1DocumentTypeListUrl = () => {




  return `/v1/document-type/list`
}

export const getV1DocumentTypeList = async ( options?: RequestInit): Promise<getV1DocumentTypeListResponseSuccess> => {

  return fetcher<getV1DocumentTypeListResponseSuccess>(getGetV1DocumentTypeListUrl(),
  {
    ...options,
    method: 'GET'


  }
);}





export const getGetV1DocumentTypeListQueryKey = () => {
    return [
    `/v1/document-type/list`
    ] as const;
    }


export const getGetV1DocumentTypeListQueryOptions = <TData = Awaited<ReturnType<typeof getV1DocumentTypeList>>, TError = ErrorResponse>( options?: { query?:UseQueryOptions<Awaited<ReturnType<typeof getV1DocumentTypeList>>, TError, TData>, request?: SecondParameter<typeof fetcher>}
) => {

const {query: queryOptions, request: requestOptions} = options ?? {};

  const queryKey =  queryOptions?.queryKey ?? getGetV1DocumentTypeListQueryKey();



    const queryFn: QueryFunction<Awaited<ReturnType<typeof getV1DocumentTypeList>>> = ({ signal }) => getV1DocumentTypeList({ signal, ...requestOptions });





   return  { queryKey, queryFn, ...queryOptions} as UseQueryOptions<Awaited<ReturnType<typeof getV1DocumentTypeList>>, TError, TData> & { queryKey: QueryKey }
}

export type GetV1DocumentTypeListQueryResult = NonNullable<Awaited<ReturnType<typeof getV1DocumentTypeList>>>
export type GetV1DocumentTypeListQueryError = ErrorResponse


/**
 * @summary List KYC document types
 */

export function useGetV1DocumentTypeList<TData = Awaited<ReturnType<typeof getV1DocumentTypeList>>, TError = ErrorResponse>(
  options?: { query?:UseQueryOptions<Awaited<ReturnType<typeof getV1DocumentTypeList>>, TError, TData>, request?: SecondParameter<typeof fetcher>}

 ):  UseQueryResult<TData, TError> & { queryKey: QueryKey } {

  const queryOptions = getGetV1DocumentTypeListQueryOptions(options)

  const query = useQuery(queryOptions) as  UseQueryResult<TData, TError> & { queryKey: QueryKey };

  return { ...query, queryKey: queryOptions.queryKey };
}






